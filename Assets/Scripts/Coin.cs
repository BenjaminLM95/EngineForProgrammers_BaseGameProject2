using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Coin : MonoBehaviour
{
    public bool StartingCoin; 
    // Start is called before the first frame update
    void Start()
    {
        StartingCoin = false; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
